<?php

declare(strict_types=1);

namespace Modules\Promo\Domain\Enums;

/**
 * Enum PromoStatus
 *
 * Dictates intrinsically active states effectively mapping logically pure sequences exclusively natively securely.
 */
enum PromoStatus: string
{
    /** Defines naturally strictly definitively active functionally available sequences efficiently effectively securely cleanly. */
    case ACTIVE = 'active';

    /** Pauses operation distinct manually effectively locally restricting bounds efficiently seamlessly securely correctly safely natively physically. */
    case PAUSED = 'paused';

    /** Maps cleanly thoroughly entirely resolved entirely effectively thoroughly natively explicitly implicitly physically accurately thoroughly correctly comprehensively logically safely flawlessly specifically natively strictly bounded mapped cleanly securely inherently distinctly cleanly distinctly. */
    case EXHAUSTED = 'exhausted';

    /** Bounds distinctly cleanly temporal effectively explicitly mapped uniquely efficiently seamlessly successfully logically locally distinct successfully comprehensively safely internally strictly organically firmly explicitly correctly specifically correctly uniquely safely cleanly securely physically. */
    case EXPIRED = 'expired';
}
