<script setup>
import { Head, Link } from '@inertiajs/vue3';
import { ref } from 'vue';
import MobileLayout from '@/Components/Mobile/MobileLayout.vue';
import ProductCard3D from '@/Components/Mobile/ProductCard3D.vue';
import { MagnifyingGlassIcon } from '@heroicons/vue24/outline';

const isMobile = ref(window.innerWidth < 768);
const categories = ['Events', 'Hotels', 'Food', 'Taxi', 'Gym', 'Clinics'];
const products = [
  { id: 1, name: 'Exclusive Billiards Lounge', price: '2,500', category: 'Events', image: 'https://images.unsplash.com/photo-1549413240-3d71249b6574?q=80&w=2070&auto=format&fit=crop' },
  { id: 2, name: 'Cyberpunk Karaoke VIP', price: '4,200', category: 'Karaoke', image: 'https://images.unsplash.com/photo-1508700115892-45ecd05ae2ad?q=80&w=2069&auto=format&fit=crop' },
  { id: 3, name: 'Zen Sauna Experience', price: '1,800', category: 'Wellness', image: 'https://images.unsplash.com/photo-1517457373958-b7bdd4587205?q=80&w=1469&auto=format&fit=crop' },
];
</script>

<template>
    <Head title="2026 Marketplace" />

    <MobileLayout v-if="isMobile">
        <!-- Mobile Hero -->
        <section class="space-y-6">
            <h1 class="text-5xl font-black text-white leading-none tracking-tighter">
                Explore <br/>
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-indigo-600">Tomorrow.</span>
            </h1>
            <div class="relative group">
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                    <MagnifyingGlassIcon class="h-6 w-6 text-blue-500/50 animate-pulse" />
                </div>
                <input type="text" placeholder="Search experiences..." 
                       class="block w-full pl-12 pr-4 py-4 bg-white/5 border border-white/10 rounded-2xl text-white outline-none focus:border-blue-500/50 backdrop-blur-3xl" />
            </div>
        </section>

        <!-- 3D Recommendation Grid -->
        <section class="space-y-6 mt-12">
            <h2 class="text-2xl font-black text-white px-2">AI Recommendations</h2>
            <div class="grid grid-cols-1 gap-8">
                <ProductCard3D v-for="product in products" :key="product.id" :product="product" />
            </div>
        </section>

        <!-- Categories horizontal -->
        <section class="flex space-x-3 overflow-x-auto pb-4 no-scrollbar mt-12">
            <button v-for="cat in categories" :key="cat" 
                    class="whitespace-nowrap px-6 py-3 rounded-xl bg-white/5 border border-white/10 text-xs font-black text-gray-400 uppercase tracking-widest active:scale-95 transition-transform">
                {{ cat }}
            </button>
        </section>
    </MobileLayout>

    <div v-else class="min-h-screen bg-black overflow-hidden relative">
        <!-- Existing Desktop Layout code from lines 21+ -->
        <nav class="relative z-10 flex items-center justify-between p-6 px-4 md:px-12 backdrop-blur-md border-b border-white/5 bg-black/50">
            <div class="flex items-center space-x-3">
                <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-blue-500 rounded-xl shadow-lg shadow-purple-500/20 flex items-center justify-center text-xl font-bold">
                    C
                </div>
                <span class="text-2xl font-black tracking-tighter uppercase italic text-white">CatVRF <span class="text-purple-500">2026</span></span>
            </div>
            <!-- ... -->
        </nav>
        <!-- (Rest of your original desktop template would be here) -->
    </div>
</template>

<style>
.no-scrollbar::-webkit-scrollbar { display: none; }
.no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
</style>

        <main class="relative z-10 pt-24 pb-32">
            <!-- Hero -->
            <div class="max-w-7xl mx-auto px-4 text-center mb-24">
                <div class="inline-flex items-center space-x-2 px-3 py-1 rounded-full bg-purple-500/10 border border-purple-500/20 text-purple-400 text-xs font-bold uppercase tracking-wider mb-6 animate-pulse">
                    <span class="w-2 h-2 rounded-full bg-purple-500"></span>
                    <span>AI Ecosystem Enabled</span>
                </div>
                <h1 class="text-6xl md:text-8xl font-black tracking-tight mb-8">
                    Управляй бизнесом <br>
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 via-blue-500 to-emerald-400">из будущего</span>
                </h1>
                <p class="text-gray-400 text-lg md:text-xl max-w-2xl mx-auto mb-12">
                    Единая Multi-tenant экосистема для малого и крупного бизнеса. AI-аналитика, гео-логистика и автоматизация операций в одном окне.
                </p>

                <!-- AI Search Input -->
                <div class="max-w-3xl mx-auto relative group">
                    <div class="absolute -inset-1 bg-gradient-to-r from-purple-600 to-blue-600 rounded-2xl blur opacity-25 group-hover:opacity-40 transition duration-1000 group-hover:duration-200"></div>
                    <div class="relative bg-black/60 backdrop-blur-xl border border-white/10 rounded-2xl p-2 flex items-center">
                        <span class="pl-4 text-2xl">🧠</span>
                        <input 
                            v-model="searchQuery"
                            type="text" 
                            placeholder="AI Поиск: 'Очереди в ресторанах Краснодара' или 'Анализ спроса на цветы'..." 
                            class="w-full bg-transparent border-none text-white focus:ring-0 placeholder:text-gray-600 text-lg py-4"
                        >
                        <button class="bg-purple-600 hover:bg-purple-500 px-8 py-4 rounded-xl font-bold transition-all shadow-xl shadow-purple-900/40">
                            Анализ
                        </button>
                    </div>
                </div>
            </div>

            <!-- Dashboard Preview (Glassmorphism 3D effect) -->
            <div class="max-w-6xl mx-auto px-4 mb-32 -rotate-2 hover:rotate-0 transition-transform duration-700">
                <div class="relative rounded-3xl overflow-hidden border border-white/10 bg-gradient-to-br from-white/5 to-transparent backdrop-blur-2xl shadow-2xl p-1 pointer-events-none select-none">
                    <div class="bg-black/90 rounded-[22px] p-8 aspect-video flex flex-col items-center justify-center relative overflow-hidden">
                        <!-- Simulated AI Map -->
                        <div class="absolute inset-0 opacity-40">
                            <div class="absolute top-1/4 left-1/4 w-32 h-32 bg-purple-500/40 blur-3xl animate-pulse"></div>
                            <div class="absolute bottom-1/3 right-1/4 w-48 h-48 bg-blue-500/30 blur-3xl"></div>
                        </div>
                        <h3 class="text-4xl font-bold text-white/20 uppercase tracking-widest italic select-none">
                            Interface Preview
                        </h3>
                    </div>
                </div>
            </div>

            <!-- Categories Grid -->
            <div class="max-w-7xl mx-auto px-4 mb-32">
                <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
                    <div v-for="cat in categories" :key="cat.slug" 
                        class="p-6 rounded-3xl bg-white/5 border border-white/5 hover:border-purple-500/40 transition-all group cursor-pointer active:scale-95 backdrop-blur-sm"
                    >
                        <div class="text-4xl mb-4 group-hover:scale-125 transition-transform duration-300">{{ cat.icon }}</div>
                        <div class="font-bold text-lg">{{ cat.name }}</div>
                        <div class="text-xs text-gray-500">{{ cat.count }} партнеров</div>
                    </div>
                </div>
            </div>

            <!-- Features -->
            <div class="max-w-7xl mx-auto px-4 grid md:grid-cols-3 gap-8">
                <div v-for="feature in stats" :key="feature.label" 
                    class="p-10 rounded-3xl bg-gradient-to-b from-white/[0.03] to-transparent border border-white/5 relative group overflow-hidden"
                >
                    <div class="text-4xl mb-6">{{ feature.icon }}</div>
                    <div class="text-5xl font-black mb-2">{{ feature.value }}</div>
                    <div class="text-gray-400 font-medium">{{ feature.label }}</div>
                    <div class="absolute -right-8 -bottom-8 text-white/[0.02] text-9xl font-black rotate-12 group-hover:rotate-0 transition-all group-hover:text-purple-500/[0.05]">
                        #TX
                    </div>
                </div>
            </div>
        </main>

        <footer class="relative z-10 border-t border-white/5 pt-20 pb-10 bg-black/80 backdrop-blur-xl">
            <div class="max-w-7xl mx-auto px-4 grid grid-cols-2 md:grid-cols-4 gap-12 mb-20">
                <div class="col-span-2">
                    <div class="flex items-center space-x-3 mb-6">
                         <div class="w-8 h-8 bg-purple-600 rounded-lg flex items-center justify-center font-bold">C</div>
                         <span class="text-xl font-bold italic tracking-tighter">CATVRF</span>
                    </div>
                    <p class="text-gray-500 max-w-sm mb-8">
                        Ультимативное решение для автоматизации бизнеса. От локальной кофейни до международной сети такси.
                    </p>
                </div>
                <div>
                    <h4 class="font-bold mb-6 text-white uppercase text-xs tracking-widest">Продукт</h4>
                    <ul class="space-y-4 text-gray-500 text-sm">
                        <li><a href="#" class="hover:text-white transition-colors">Маркетплейс</a></li>
                        <li><a href="#" class="hover:text-white transition-colors">Экосистема B2B</a></li>
                        <li><a href="#" class="hover:text-white transition-colors">Аналитика AI</a></li>
                    </ul>
                </div>
                <div>
                     <h4 class="font-bold mb-6 text-white uppercase text-xs tracking-widest">Компания</h4>
                    <ul class="space-y-4 text-gray-500 text-sm">
                        <li><a href="#" class="hover:text-white transition-colors">О нас</a></li>
                        <li><a href="#" class="hover:text-white transition-colors">Безопасность</a></li>
                        <li><a href="#" class="hover:text-white transition-colors">Контакты</a></li>
                    </ul>
                </div>
            </div>
            <div class="max-w-7xl mx-auto px-4 text-center text-gray-700 text-xs mt-10">
                © 2026 CatVRF Ecosystem. Built for the future of Russian Marketplace economy.
            </div>
        </footer>
    </div>
</template>

<style scoped>
@supports (-webkit-backdrop-filter: none) or (backdrop-filter: none) {
  .backdrop-blur-md {
    -webkit-backdrop-filter: blur(12px);
    backdrop-filter: blur(12px);
  }
}
</style>
