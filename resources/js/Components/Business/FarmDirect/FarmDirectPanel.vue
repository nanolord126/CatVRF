<script setup>
import { ref } from 'vue'; import VTabs from '../../UI/VTabs.vue'; import VCard from '../../UI/VCard.vue'; import VBadge from '../../UI/VBadge.vue'; import FarmDirectDashboard from './FarmDirectDashboard.vue';
const activeTab = ref('dashboard'); const tabs = [{ key: 'dashboard', label: 'Дашборд', icon: '📊' }, { key: 'suppliers', label: 'Поставщики', icon: '🚜' }, { key: 'logistics', label: 'Логистика', icon: '🚚' }];
</script>
<template><section class="space-y-4"><header class="flex items-center justify-between"><div><h2 class="text-xl font-bold" style="color: var(--t-text);">FarmDirect Panel</h2><p class="text-sm" style="color: var(--t-text-3);">Фермерские поставки и свежесть.</p></div><VBadge text="PRODUCTION" variant="success" size="sm" dot /></header><VTabs v-model="activeTab" :tabs="tabs" variant="segment" size="sm" /><FarmDirectDashboard v-if="activeTab==='dashboard'" /><VCard v-else-if="activeTab==='suppliers'" title="Поставщики"><div class="text-sm" style="color: var(--t-text-2);">Контракты, качество партий, приемка.</div></VCard><VCard v-else title="Логистика"><div class="text-sm" style="color: var(--t-text-2);">Холодовая цепь, ETA, spoilage control.</div></VCard></section></template>
