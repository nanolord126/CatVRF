<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $u = App\Models\User::firstOrCreate(['email' => 'admin@catvrf.local']); $t = Illuminate\Support\Facades\DB::table('tenants')->first(); if ($t && !empty($t->id)) { $u->tenant_id = $t->id; } $u->password = Hash::make('password'); $u->is_admin = true; $u->save(); echo 'User created/updated: ' . $u->email . ' with tenant_id: ' . $u->tenant_id; 
