<?php declare(strict_types=1);

/**
 * КОНФИГУРАЦИЯ БИЗНЕС-КАНАЛОВ (КАНОН 2026)
 *
 * Использование:
 *   config('channels.plans.basic.price_kopecks')
 *   config('channels.allowed_reactions')
 */
return [

    // =========================================================================
    // Тарифные планы
    // =========================================================================
    'plans' => [

        'basic' => [
            'name'            => 'Базовый',
            'price_kopecks'   => (int) env('CHANNEL_BASIC_PRICE_KOPECKS', 4900),   // 49₽
            'posts_per_day'   => 2,
            'photos_per_post' => 5,
            'shorts_enabled'  => true,   // видео и shorts разрешены
            'polls_enabled'   => false,
            'promo_enabled'   => false,
            'advanced_stats'  => false,  // только просмотры, реакции, подписчики
            'scheduled_posts' => true,
        ],

        'extended' => [
            'name'            => 'Расширенный',
            'price_kopecks'   => (int) env('CHANNEL_EXTENDED_PRICE_KOPECKS', 19900), // 199₽
            'posts_per_day'   => 5,
            'photos_per_post' => 20,
            'shorts_enabled'  => true,
            'polls_enabled'   => true,
            'promo_enabled'   => true,
            'advanced_stats'  => true,  // CTR, время просмотра, гео, демография
            'scheduled_posts' => true,
        ],
    ],

    // =========================================================================
    // Разрешённые реакции (нормы РФ — без экстремизма и запрещённых символов)
    // Ключ = emoji codepoint, значение = русское название
    // =========================================================================
    'allowed_reactions' => [
        '👍' => 'Нравится',
        '❤️' => 'Любовь',
        '🔥' => 'Огонь',
        '😊' => 'Улыбка',
        '👏' => 'Аплодисменты',
        '😮' => 'Удивление',
        '😢' => 'Грусть',
        '🤔' => 'Интересно',
        '💡' => 'Идея',
        '⭐' => 'Отлично',
    ],

    // =========================================================================
    // Архивация неактивных каналов
    // =========================================================================
    'archive' => [
        // Через сколько дней без постов архивировать канал
        'inactive_days' => (int) env('CHANNEL_ARCHIVE_INACTIVE_DAYS', 90),

        // Предупреждение за N дней до архивации
        'warn_before_days' => 14,
    ],

    // =========================================================================
    // Лимиты и безопасность
    // =========================================================================
    'limits' => [
        // Rate-limit: максимум попыток реакций в минуту на user
        'reactions_per_minute' => 30,

        // Максимальное количество каналов на один тенант
        'max_channels_per_tenant' => 1,

        // Максимальная длина поста (символов)
        'max_post_length' => 10000,

        // Максимальный размер одного медиафайла (байт): 50MB
        'max_media_size_bytes' => 52_428_800,
    ],

    // =========================================================================
    // Модерация
    // =========================================================================
    'moderation' => [
        // Требовать модерацию перед публикацией
        'enabled' => (bool) env('CHANNEL_MODERATION_ENABLED', true),

        // SLA модерации в часах
        'sla_hours' => (int) env('CHANNEL_MODERATION_SLA_HOURS', 4),
    ],

    // =========================================================================
    // Кэш
    // =========================================================================
    'cache' => [
        // TTL кэша поста (сек)
        'post_ttl'    => 300,

        // TTL кэша ленты новостей
        'feed_ttl'    => 120,

        // TTL кэша статистики
        'stats_ttl'   => 600,

        // TTL кэша счётчика подписчиков
        'subs_ttl'    => 300,
    ],

    // =========================================================================
    // Уведомления
    // =========================================================================
    'notifications' => [
        // Отправлять push-уведомления подписчикам при новом посте
        'push_on_publish' => (bool) env('CHANNEL_PUSH_ON_PUBLISH', true),

        // Уведомить бизнес за N дней до окончания тарифа
        'plan_expiry_warn_days' => 3,
    ],
];
