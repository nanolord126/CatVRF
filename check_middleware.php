<?php require __DIR__."/vendor/autoload.php"; $app = require_once __DIR__."/bootstrap/app.php"; $kernel = $app->make(Illuminate\Contracts\Http\Kernel::class); $request = Illuminate\Http\Request::create("/superadmin/login", "GET"); $response = $kernel->handle($request); $route = $request->route(); if ($route) { echo implode("\n", $route->middleware()); } else { echo "Route not found during full handle."; }
